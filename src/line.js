import React from 'react';
import { createRoot } from 'react-dom/client';
import LineExample from '../examples/LineExample';

const root = createRoot(document.getElementById('root'));
root.render(<LineExample />);
