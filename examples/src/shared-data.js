import React from 'react';
import { createRoot } from 'react-dom/client';
import SharedDataExample from '../SharedDataExample';

const root = createRoot(document.getElementById('root'));
root.render(<SharedDataExample />);
