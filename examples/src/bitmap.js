import React from 'react';
import { createRoot } from 'react-dom/client';
import BitmapExample from '../BitmapExample';

const root = createRoot(document.getElementById('root'));
root.render(<BitmapExample />);
